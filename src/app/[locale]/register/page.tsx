import { FC } from 'react';
import ProfileContainer from '@/components/containers/ProfileContainer';

const RegisterPage: FC = () => <ProfileContainer view={'register'} />;

export default RegisterPage;
