import { FC } from 'react';
import InformationContainer from '@/components/containers/InformationContainer';

const ContactPage: FC = () => <InformationContainer view={'contact'} />;

export default ContactPage;
